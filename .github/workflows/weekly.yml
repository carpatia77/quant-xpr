name: Weekly Ingestion
on:
  schedule:
    - cron: '0 0 * * 0'
jobs:
  ingest:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Run ingestion
        run: echo "Running ingestion..."
